[build-system]
requires = ["setuptools>=83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "portal"
version = "0.1.0"
description = "Web portal and worker for managing fetch jobs."
requires-python = ">=3.11"
dependencies = [
  "asyncpg>=0.31.0",
  "cryptography>=50.0.0",
  "fetch",
  "httpx>=0.28.1",
  "jinja2>=3.1.6",
  "jinjax>=0.65",
  "litestar>=2.24.0",
  "litestar-htmx>=0.5.0",
  # The worker API protocol imports msgspec at runtime.
  "msgspec>=0.19.0",
  "psutil>=7.0.0",
  "pwdlib[argon2]>=0.3.0",
  "pyotp>=2.10.0",
  "segno>=1.6.6",
  "uvicorn>=0.52.1",
  "webauthn>=3.0.0",
]

[project.scripts]
portal = "portal.cli:main"

[tool.uv.sources]
fetch = { workspace = true }

[tool.setuptools.packages.find]
include = ["portal*"]

[tool.setuptools.package-data]
portal = [
  "migrations/*.sql",
  "web/components/*.css",
  "web/components/*.jinja",
  "web/pages/*.jinja",
  "web/static/*.css",
  "web/static/*.js",
  "web/static/*.woff2",
]

[tool.djlint]
profile = "jinja"
indent = 2
extension = "jinja"
custom_html = "[A-Z][a-zA-Z0-9]*" # Preserve JinjaX component tags.
