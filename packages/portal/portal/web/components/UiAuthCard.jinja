{#def title, description, title_id #}
<section class="acceso" aria-labelledby="{{ title_id }}">
  <div class="acceso__marca" aria-hidden="true">W</div>
  <h1 id="{{ title_id }}">{{ title }}</h1>
  <p class="acceso__descripcion">{{ description }}</p>
  {{ content }}
</section>
