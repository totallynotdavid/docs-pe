{#def title, description #}
<div {{ attrs.render(class="query-flow") }}>
  <header class="query-flow__header">
    <h1>{{ title }}</h1>
    <p>{{ description }}</p>
  </header>
  {{ content }}
</div>
