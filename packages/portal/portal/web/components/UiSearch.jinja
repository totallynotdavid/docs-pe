{#def action, query="" #}
<form {{ attrs.render(class="busqueda", method="get", action=action) }}>
  <label for="consulta">Número de documento</label>
  <input id="consulta"
         name="q"
         value="{{ query }}"
         autocomplete="off"
         placeholder="Ej. 10412345678">
  <button type="submit">Buscar</button>
</form>
