{#def title, description, title_id #}
<section {{ attrs.render(class="auth-card", aria_labelledby=title_id) }}>
  <div class="auth-card__brand" aria-hidden="true">W</div>
  <h1 id="{{ title_id }}">{{ title }}</h1>
  <p class="auth-card__description">{{ description }}</p>
  {{ content }}
</section>
