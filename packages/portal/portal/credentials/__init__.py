"""Secret protection for stored proxy credentials.

The field schema lives in `fetch.proxy.registry`, so the portal renders a form for
any vendor the engine can drive without restating its fields.
"""
